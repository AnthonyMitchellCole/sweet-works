# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.Python
.venv/
venv/
env/
.env

# --- Tooling caches --------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.benchmarks/
.coverage
.coverage.*
htmlcov/

# --- Project: procedurally generated sprite cache --------------------------
# Rebuilt by `make sprites` (or on first launch by AssetLoader.prepare).
assets/sprites/t*_i*/

# --- Project: optional sprite spec overrides -------------------------------
# Saved by the in-game Sprite Studio (F4); per-developer.
assets/sprites/overrides.json

# --- Project: persisted user settings --------------------------------------
# Per-machine display / audio / sim tuning; written by the Settings scene.
assets/user_settings.json

# --- OS / IDE --------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
