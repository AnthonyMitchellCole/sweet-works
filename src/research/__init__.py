"""Research tree: data, state, and UI projection.

Pure-data modules (``node``, ``tree``, ``state``, ``info``) keep the
research feature independent of pygame so the sim and unit tests can
import it without pulling in the render stack.
"""
